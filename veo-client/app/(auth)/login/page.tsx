import Login from "@/components/Login";

const page = () => {
  return (
    <section className="min-h-screen flex items-center justify-center">
      <Login />
    </section>
  );
};

export default page;
